import { createAction } from '@ngrx/store';

// Example action, replace with actual if needed
export const navigateToApplyNow = createAction('[Blocked Account] Navigate To Apply Now');
export const navigateToSecureMail = createAction('[Blocked Account] Navigate To Secure Mail');
